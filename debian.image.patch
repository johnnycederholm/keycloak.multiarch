diff --git a/server/Dockerfile b/server/Dockerfile
index d28336f..1bfd85b 100644
--- a/server/Dockerfile
+++ b/server/Dockerfile
@@ -1,6 +1,6 @@
-FROM registry.access.redhat.com/ubi8-minimal
+FROM debian:buster-slim
 
-ENV KEYCLOAK_VERSION 12.0.1
+ENV KEYCLOAK_VERSION 12.0.4
 ENV JDBC_POSTGRES_VERSION 42.2.5
 ENV JDBC_MYSQL_VERSION 8.0.22
 ENV JDBC_MARIADB_VERSION 2.5.4
@@ -17,7 +17,10 @@ ARG KEYCLOAK_DIST=https://github.com/keycloak/keycloak/releases/download/$KEYCLO
 
 USER root
 
-RUN microdnf update -y && microdnf install -y glibc-langpack-en gzip hostname java-11-openjdk-headless openssl tar which && microdnf clean all
+#RUN microdnf update -y && microdnf install -y glibc-langpack-en gzip hostname java-11-openjdk-headless openssl tar which && microdnf clean all
+RUN mkdir -p /usr/share/man/man1
+RUN apt-get update -y && apt install -y curl gzip hostname openjdk-11-jre-headless openssl tar && apt autoremove
+RUN /usr/bin/c_rehash
 
 ADD tools /opt/jboss/tools
 RUN /opt/jboss/tools/build-keycloak.sh
